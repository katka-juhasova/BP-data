local ParquetEnvelopeReader = require 'parquet.ParquetEnvelopeReader'
local ParquetReader = require 'parquet.ParquetReader'

local M = {
  ParquetEnvelopeReader = ParquetEnvelopeReader,
  ParquetReader = ParquetReader
}

return M
