Help [[

math.atan2 (x, y)
-----------------

  Returns the arc tangent of `x/y` (in radians), but uses the signs of both
  parameters to find the quadrant of the result. (It also handles correctly
  the case of `y` being zero.)
]]
