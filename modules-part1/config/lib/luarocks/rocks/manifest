commands = {}
dependencies = {
   config = {
      ["1.0.0-1"] = {
         {
            constraints = {
               {
                  op = ">=",
                  version = {
                     5, 1, string = "5.1"
                  }
               }
            },
            name = "lua"
         }
      }
   }
}
modules = {
   config = {
      "config/1.0.0-1"
   }
}
repository = {
   config = {
      ["1.0.0-1"] = {
         {
            arch = "installed",
            commands = {},
            dependencies = {},
            modules = {
               config = "config.lua"
            }
         }
      }
   }
}
